{
  "name": "@selgetabel/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host localhost --port 8000",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "cli": "uv run python cli.py",
    "install": "uv sync"
  }
}
