# ===========================================
# Docker Compose 与 API 环境变量模板
# 复制为 .env 并填写实际值：cp .env.example .env
# ===========================================

# ---- OpenAI ----
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# ---- PostgreSQL（postgres 服务 + API 连接）----
# USER/DB 可不填，默认 llmexcel；密码必填
POSTGRES_USER=llmexcel
POSTGRES_PASSWORD=
POSTGRES_DB=llmexcel

# ---- MinIO ----
# USER 可不填，默认 minioadmin；密码必填
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=

# ---- API：JWT / 加密 ----
# 生产环境务必设置 JWT_SECRET_KEY，例如：openssl rand -hex 32
JWT_SECRET_KEY=
BCRYPT_ROUNDS=12
